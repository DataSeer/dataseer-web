extends ../../shared/layout.pug

block head
  link(rel="stylesheet" type="text/css" href=conf.root + "/views/signin/app.css")

block app
  include ./app.pug

block scripts
  script(src=conf.root + "/views/signin/app.js")