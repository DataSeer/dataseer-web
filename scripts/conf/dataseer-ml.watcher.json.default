{
  "enabled": true,
  "timeout": 60000,
  "url": "http://localhost:8060",
  "cmd": "docker rm -f dataseer-ml; docker run --name dataseer-ml -d -it -p 8060:8060 --init dataseer/dataseer:0.7.2-SNAPSHOT"
}